import 'package:flutter/material.dart';

class BlackjackPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Blackjack'),
      ),
      body: Center(
        child: Text('Bienvenue à la page du blackjack'),
      ),
    );
  }
}
