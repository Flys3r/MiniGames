import 'package:flutter/material.dart';

class SolitairePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Solitaire'),
      ),
      body: Center(
        child: Text('Bienvenue à la page du solitaire'),
      ),
    );
  }
}
